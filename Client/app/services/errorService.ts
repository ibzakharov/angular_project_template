class ErrorService {
    public static inject: string[] = [

    ];

    constructor() {
    }

    public serverNotAvailable() {
        
    }

}

angular.module($app.name).service('error', ErrorService);   